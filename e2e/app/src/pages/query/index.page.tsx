import { definePage } from "@pajecawav/yamf";
import { QueryDemo } from "./QueryDemo.island";

export default definePage({
	render: async () => {
		return (
			<QueryDemo
			// initialData={123}
			/>
		);
	},
});
